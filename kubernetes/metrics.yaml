apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-chat-metrics
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: ai-chat
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
